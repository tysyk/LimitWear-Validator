[pytest]
testpaths = tests
addopts = -ra
markers =
    unit: focused business logic tests that avoid model loading
    integration: tests that exercise real pipeline/API/detector components
    evaluation: dataset-driven expected verdict tests
    slow: tests that load ML/OCR models or run the full pipeline
filterwarnings =
    ignore:You are using `torch.load`:FutureWarning
